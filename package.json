{
  "name": "quotes",
  "private": true,
  "scripts": {
    "build": "spago build && vite build",
    "compile": "spago build",
    "preview": "spago build && vite preview",
    "repl": "spago repl",
    "start": "spago build && vite --open",
    "test": "spago test"
  },
  "devDependencies": {
    "bootstrap": "^5.3.3",
    "vite": "^5.4.1"
  }
}
