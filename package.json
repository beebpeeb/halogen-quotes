{
  "name": "quotes",
  "private": true,
  "scripts": {
    "build": "spago build && vite build",
    "compile": "spago build",
    "preview": "spago build && vite preview",
    "repl": "spago repl",
    "start": "spago build && vite --open",
    "test": "spago test"
  },
  "devDependencies": {
    "bootstrap": "^5.3.5",
    "bootswatch": "^5.3.5",
    "purescript": "^0.15.15",
    "purs-tidy": "^0.11.0",
    "spago": "^0.93.43",
    "vite": "^6.3.3"
  }
}
